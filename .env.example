# ===========================================
# NOXZIPPER ENVIRONMENT VARIABLES
# ===========================================

# Database - Choose ONE option:
# -----------------------------------------
# Option 1: Local SQLite (development only)
DATABASE_URL="file:./dev.db"

# Option 2: Turso (SQLite cloud - recommended for Vercel)
# DATABASE_URL="libsql://your-database.turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# Option 3: Vercel Postgres
# DATABASE_URL="postgres://user:pass@host:5432/db?sslmode=require"

# Option 4: PlanetScale (MySQL)
# DATABASE_URL="mysql://user:pass@host/db?sslaccept=strict"

# NextAuth Configuration
# -----------------------------------------
# Generate secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
# For Vercel: NEXTAUTH_URL is auto-detected, but set it for custom domains

# Admin credentials (used by seed script)
# -----------------------------------------
ADMIN_EMAIL="admin@noxzipper.com"
ADMIN_PASSWORD="admin123"

# SMTP Configuration (optional - for email sending)
# -----------------------------------------
# Leave blank to use copy-to-clipboard fallback
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASSWORD=""
SMTP_FROM=""
